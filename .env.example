DB_HOST=localhost
DB_USER=picncic_user
DB_PASSWORD=UnSecure-PAssword
DB_NAME=picnic_app
PORT=3000
# Secret token for the admin interface (/admin.html). Leave unset to disable
# the admin API entirely. Generate one with: openssl rand -hex 24
ADMIN_TOKEN=
